<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectoindividual">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Usuarios.RegistroActivity"></activity>

        <activity android:name=".Usuarios.PrincipalUsuarioActivity" />
        <activity android:name=".VisorPdfActivity" />
        <activity android:name=".VisorFotoActivity" />
        <activity android:name=".Administrador.NuevoArchivoActivity" />
        <activity android:name=".CategoriaActivity" />
        <activity android:name=".ListaArchivosPrincipalActivity" />
        <activity android:name=".ListaArchivosFavoritosActivity" />
        <activity android:name=".LogueoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>